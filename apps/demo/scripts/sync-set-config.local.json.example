{
  "origin": "http://localhost:3210",
  "deployKey": "REEMPLAZAR — `docker compose exec -T backend ./generate_admin_key.sh` → línea notchat-local|...",
  "destination": {
    "kind": "duckdb_local",
    "path": "/data/local.duckdb"
  }
}
