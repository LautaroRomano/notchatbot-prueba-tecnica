/**
 * Convex empaqueta `schema.ts` en la raíz del directorio del componente
 * (junto a `convex.config.ts`). El modelo vive en `src/component/schema.ts`.
 */
export { default } from "./src/component/schema";
